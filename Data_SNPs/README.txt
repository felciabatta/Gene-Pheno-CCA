This folder should only contain .txt files exported using plink2, which are generated semi-automatically using get_snpdata.m

The files contain individual genome data for a selection SNPs from a specified sub population, to match the GWAS ATLAS dataset being analysed.

The naming of these files is produced during the use of get_snpdata.m